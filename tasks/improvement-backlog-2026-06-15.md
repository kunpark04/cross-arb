# Improvement backlog — cross-arb live bot (2026-06-15)

Generated by a multi-agent survey (`improvement-backlog` workflow, 6 agents / 5 survey angles: code
TODOs+deferred, decision log, research/CLAUDE.md findings, `tasks/todo.md`, live-money test gaps) then
adversarially prioritized. **53 candidates → 17 kept, 25 dropped** as already-done / duplicate / noise.

> **Verification status:** agent-surveyed with `file:line` citations; **not yet independently verified**.
> Treat as a triage list — verify the specific item before acting. Captured while the bot runs LIVE at
> cap-5 (1 initial + 4 scale-in/re-entry adds, weather-only until sports/econ recon).

## Tier 1 — HIGH

| # | Item | Cat | Effort |
|---|---|---|---|
| 1 | **pmus WS-reconnect has no per-slug freshness gate** (asymmetric vs Kalshi `k_fresh`). pmus reconnect only sets `pm_rebuild`, leaves old `PmusBook` entries un-cleared, and un-pauses ALL pmus slugs on the first frame of ANY slug (`live.rs:271-273,288`). A stale-but-<5s pmus quote can fire against a fresh Kalshi book — on the leg that binds ~9× more often. **Fix:** add a `pm_fresh` set + clear `pmus_books` on pmus Reconnect, mirror `all_fresh`. | live-money | M |
| 2 | **Naked-leg recovery UNVERIFIED LIVE end-to-end** — FOK-kill-on-miss, pmus SELL recovery signing (doc-only `exec.rs:546`), and a real Kalshi FOK partial were never observed under a real first-leg miss. The next 1-contract live FOK fire IS the verification (0023). **Tonight's cap-5 weather run exercises exactly this path.** | live-money | M |
| 3 | **Confirmatory multi-week validation gate NOT cleared** (governance, not code) — 0015's staged rollout gates production on the 0014 confirmatory run (≥21 post-ECON_REMAP event-days, ≥30 τ-clearing candidates) AND a measured naked-unwind cost. Sports/econ recons finalize ~06-23/25 and 07-02. Scaling beyond 1-contract before this is the exact risk 0015's protest-of-record warns against. | measurement | L |

## Tier 2 — MED

**live-money-correctness**
- **Recovery-cost gate fails OPEN on a one-sided first-fired book** (`risk.rs:249` wraps the 0020 gate in `if let (Some,Some)`) — the bot ENTERS an arb it should skip → kill-switch halt + manual flatten instead of a clean skip. Fix: reject when bid/ask is `None`. *(S)*
- **Both-legs-partial GTC recovery SELL can fill AFTER the halt** — reachable only at >1 contract, explicitly unverified; a resting GTC SELL re-flattens/over-sells untracked. Flag before scaling past 1. *(M)*
- **Dynamic fire-order thinness uses TOTAL ask-volume, not depth-at-edge** (`book.rs:287,302`) — mis-ranks a deep-overall/thin-at-touch leg; the 0025 known-INFO. Harmless at 1 ctr, revisit before a fatter single clip. Fix: rank by fillable qty at trade size. *(S)*
- **Settlement-identity gate (0018) not wired into the live trade path** — runs only via `--audit`; prerequisites (soccer Tie-bind, `atc-` `void_haircut`) unmet, so the 177 WC pairs stay conservatively gated. *(M)*

**measurement-integrity**
- **`edge_rate` not threaded into the persisted order record** — `MIN_EDGE_RATE_CPD` gate is ENABLED at 1.0 but uncalibrated/flying-blind (the live approve path passes `cost_per`, not `edge_rate`). *(S)*
- **Entry/recovery book-snapshot incomplete** — can't decompose naked-leg cost into spread-vs-adverse-move, which is the explicit final gate before scaling (0015). Fix: snapshot bid/ask/depth at entry AND recovery. *(S)*

**pnl-scaling**
- **All-in edge cost terms not in the live filter** (0010) — bot filters only taker-fee-net edge; size-aware slippage, latency haircut, leg-fill EV unbuilt. Gating filter quality for any scale-up. *(L)*
- **Maker-side execution mode unbuilt** — rest-on-Kalshi weather ($0 maker fee) + taker-hedge pmus is the only +EV maker config (+0.14–0.44¢/attempt); the largest queued PnL lever. Gated behind first measuring fill rates from the new logging. *(L)*

## Tier 3 — LOW
- Backfill the **missing decision 0016** (live venue-contract verification) + lesson L27 — doc debt. *(S)*
- pmus catalog page-cap truncation only WARNs (could trade an incomplete universe) — fail-loud-or-stop. *(S)*
- Per-slug abort cooldown coupled to `entry_cooldown_s` — no independent abort counter (only bites if cooldown=0). *(S)*
- Econ release-calendar feed absent — edge-rate ranks econ via a flat 21d fallback (near-zero cost until econ trades 07-02). *(M)*
- Soccer `fifa`/`mls` unmapped — forgone breadth, out-of-season now (loud, never silent). *(M)*
- Monitor FLIP debounce (`monitor.py:150`) — reversals split one episode into CLOSE+OPEN, biasing the measurement rig. *(M)*

## Dropped as already-done / dupe / noise (25)
Confirmed DONE: recovery-wired-into-loop, aggressive-second-leg, Kalshi float-count fix (`634c5f1`),
postpone/unwind (`auto_unwind` ON), pmus ENTRY POST-signing (live-verified 06-11), sports date+doubleheader
guard, H1 toxicity-direction gate. Merged duplicates: the sub-second/allocation/recon/maker-bound research
rows all folded into the validation-gate or maker-mode items. Noise: MLB line-lag n=1 (instrument-don't-bet),
scale-in arming (owner action, not eng task), politics coverage (long-dated). Stale: demo-sandbox session
(no demo access), Python ledger.py trade-selection (superseded by bot-rs / 0015).
