# Files deployed to the Linux droplet — keep LF endings (a `#!/usr/bin/env bash\r` shebang fails
# "bad interpreter"; a CRLF systemd unit is iffy). See deploy/. (.ps1 are Windows-only — leave them.)
*.sh      text eol=lf
*.service text eol=lf
