# secrets & credentials
scripts/.env
*.pem
*.key

# raw API pulls / scratch data
scripts/_data/

# python
__pycache__/
*.pyc

# rust (bot-rs) — build artifacts only; src + Cargo.toml + Cargo.lock ARE tracked
bot-rs/target/
